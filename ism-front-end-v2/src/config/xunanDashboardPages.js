/**
 * 兼容旧 import。固定项目、组织名称和页面 ID 映射已停用；
 * 运行态统一从 /displayModelTemplateMap 与 monitortree 获取数据。
 */
export const XUNAN_MODEL_ID = ''
export const XUNAN_PROJECT_UUID = ''
export const XUNAN_SUBSTATION_PAGE_MAP = Object.freeze({})

export function resolveXunanSubstationPageId() {
  return null
}
