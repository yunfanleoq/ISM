---
description: 防止修改无关稳定代码并避免已修复功能回归
alwaysApply: true
---

# 已验证代码防回归铁律

## 核心原则

用户已纠正且验证正确的代码是稳定基线。后续处理其他需求时，**只改本次明确指定的部分**；不得因“顺手”修改而重新破坏既有正确行为。

## 强制要求

- 禁止对已验证正确的代码进行顺手重构、“顺带优化”、统一风格改写或无关清理。
- 仅修改用户明确指定的文件、函数或行为；需求未覆盖的代码保持不动。
- 修改前先确认目标范围。若相邻代码看起来可改进但不属于本次需求，不要动。
- 特别保护以下易回退区域：
  - `ISMBase` 组件注册链路；
  - `ViewRealTable`、`ViewSvgText` 等组态组件；
  - `actions.js` 中的 `parseRawPageLayerFields`、`applyNavContextToPageConfig`，以及相关的 import / export。
- 如确实必须触及已稳定区域，先向用户说明必要原因、影响范围与风险，并将 diff 最小化；不得借此扩展改动范围。

## 提交前自检

1. 本次 diff 是否只包含用户指定的目标？
2. 是否改动了已验证正确、但与需求无关的代码？
3. 易回退区域若被触及，是否有不可替代的理由且改动最小？

任一答案不满足要求时，撤回无关改动后再继续。
