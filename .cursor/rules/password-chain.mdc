---
description: ISM 密码加密传输验证全链路铁律 —— 保护 MD5 + bcrypt 一致性，禁止随意改动任一环节
alwaysApply: true
globs: "**/*.go,**/*.vue,**/*.js"
---

# ISM 密码处理全链路（禁止随意修改）

## 核心链路（五步走，一步都不能改）

```
用户输入原始密码 → 前端 MD5 → POST /api/login → 后端 bcrypt.CompareHashAndPassword(库hash, MD5值)
```

| 步骤 | 位置 | 代码 | 说明 |
|------|------|------|------|
| 1 | `Login.vue:155` | `md5(this.form.getFieldValue('password'))` | 前端必须 MD5 |
| 2 | `user.js:13-17` | `{Username: name, password: passwordMd5}` | 字段名 `password` 传 MD5 值 |
| 3 | `vue.config.js:54-57` | `/api` proxy → `http://127.0.0.1:8081/` | pathRewrite 去掉 `/api` |
| 4 | `user.go:321-334` | `CheckLogin(username, password)` | password 参数就是 MD5 值 |
| 5 | `user.go:289-297` | `bcrypt.CompareHashAndPassword(hashedPwd, plainPwd)` | bcrypt 比较 |

## 数据库密码字段存储格式

```
password 字段 = bcrypt(MD5(原始密码))
```

**也就是说：数据库中的 password 永远是 bcrypt 对 MD5 值加密的结果，不是对原始密码加密的结果。**

## 密码写入/重置的正确姿势

### ✅ 正确（前端发来 MD5 → 后端直接 bcrypt）

```go
// 这是对的 —— 前端已经 MD5 了，后端直接 bcrypt 即可
ScryptPw(frontendMD5Value) // = bcrypt(MD5(原始密码))
```

来自 `user.go:242-249` SetUserPassword —— 这里的 `NewPassword` 参数已经从前端收到的是 MD5 值，所以正确。

### ❌ 错误陷阱（重置密码直接 bcrypt 原始密码）

```go
// 这是错的！—— 没有经过 MD5 层
ScryptPw("123456") // = bcrypt("123456") ≠ bcrypt(MD5("123456"))
```

**受影响的代码位置：**

1. `user.go:234` `ResetPwd()` — 直接 `ScryptPw("123456")`，没有先 MD5
2. `db.go:191` admin 初始化 — 硬编码的 hash 值只对应 `bcrypt("123456")`，缺少 MD5 层

### 正确重置密码的方式

```go
import "crypto/md5"

func ResetPwd(id int) int {
    md5Password := fmt.Sprintf("%x", md5.Sum([]byte("123456")))
    err := Db.Model(&User{}).Where("id = ?", id).Update("password", ScryptPw(md5Password)).Error
    // ...
}
```

## 故障现象速查

当数据库 password ≠ bcrypt(MD5(原始密码)) 时，无论输入什么密码都会提示"密码错误"，因为前端永远发 MD5 值。

## 排障命令

```bash
# 查看当前 admin 的密码 hash
sqlite3 data/db/ism.db "SELECT id, username, password FROM user WHERE username='admin';"

# 测试原始密码直接登录（跳过前端 MD5，验证 bcrypt 层是否正常）
curl -s -X POST http://127.0.0.1:8081/login \
  -H 'Content-Type: application/json' \
  -d '{"Username":"admin","password":"123456"}'

# 测试 MD5 密码登录（模拟前端完整链路）
curl -s -X POST http://127.0.0.1:8081/login \
  -H 'Content-Type: application/json' \
  -d "{\"Username\":\"admin\",\"password\":\"$(echo -n '123456' | md5)\"}"
```

## 严禁事项

- **禁止** 在前端去掉 MD5 层
- **禁止** 在后端 `CheckLogin` 里对 password 再做一次 MD5（那会变成 bcrypt(MD5(MD5(原始密码))))
- **禁止** 在 `ResetPwd` / `db.go` 初始化时直接用 `ScryptPw("123456")` —— 必须先 `md5.Sum`
- **禁止** 修改 `vue.config.js` 的 `/api` proxy 路径规则，除非前端 `api.js` 同步改
