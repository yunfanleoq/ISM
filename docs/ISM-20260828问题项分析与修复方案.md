# ISM 20260828 问题项：分析结论、方案与待办

> 需求来源：现场 `20260828问题项.doc`（导航、历史备份/入库查询、Modbus 全量导出、备份删除）。  
> 计划副本：`.cursor/plans/ism-20260828问题项分析与修复方案.plan.md`  
> 对照包：0827 `b3c231f1` 已覆盖导航双发与点表 UUID；本轮补缺口并打 0828 完整包。

不改：密码链、`ISMBase`、`parseRawPageLayerFields` / `applyNavContextToPageConfig` 主体、不关 `native-bitunpack`、不覆盖现场 `conf/` / OceanBase / TDengine 数据。

## 对照总表

| # | 现场现象 | 判断 | 本轮 |
|---|---|---|---|
| 1 | 绑三页；不勾弹窗不跳；勾弹窗报「找不到页面」 | 0827 已去掉 `MenuConfigPage`+`ChargePage` 双发。弹窗走 `selectPopUpDisplayPageDataStruct`，没有主路径已有的 `homeFromModelUuid` | **弹窗路径同样：`pageid===displayUUID` 时跳该模型首页** |
| 2a | 历史库备份：`docker exec -T` → `unknown shorthand flag: 'T'`（8/28 17:26，0827 包上已复现） | `-T` 是 compose 标志，麒麟 `docker exec` 没有 | **去掉三处 `-T`** |
| 2b | 2208 点定时 600s，日志 `wrote` 有、界面查不到 | `wrote` 只是入队；入库根因是旧随机 TAGS，0826 已修。查询页不是新 bug | **README 排查步骤；不改查询 API** |
| 3 | Modbus「导出全量点位」超时、网页崩溃 | 浏览器按模型/组/寄存器 N+1 拉再拼 Excel | **对齐虚拟设备：后端一次 JOIN 出 xlsx** |
| 4 | 实时库还原列表要「删除」 | 曾在 `29ef2632` 落地后被冲掉 | **恢复 `DbDeleteBackup`，只删 `data/dbbackup/`** |
| 附 | 按位有的 `-` 能用、有的必须改 `_` | 0827 只在 settle 互认；实时 `ApplySource` 精确匹配 | **实时路径同样互认，打进本包** |

## 本轮代码

1. `actions.js` `selectPopUpDisplayPageDataStruct`：`pageid===displayUUID` 时 `resolveModelHomePageUuid` 后递归本函数。
2. `hisDbOpt.go`：三处 `docker exec -T` 改为普通 `docker exec`；密码 `-p` 紧贴不动。
3. `ExportAllModbusDataModel` + 前端单次 blob（2h timeout）。
4. `DbDeleteBackup`：只删 `data/dbbackup/` 内普通文件；还原列表二次确认。
5. `bitunpack` `ApplySource` 走 `sourceLookupPairs`；列头拆开后的短名也做 `-`/`_` 互换。

## 历史查询排查（2b，不改 API）

1. `grep "write history data batch failed\|write TDengine history" ism.log` — 有则入库失败（确认现场是否已是 0826 的 `TAGS(1)` 二进制）
2. `grep "timed history snapshot tick" ism.log` 看 `wrote` / `notDue` / `intervalSec` — `wrote` ≠ 已落盘
3. 历史报表按**入库当天 + 具体测点 UUID**查，不要用单个 ECC 电能代表 2208 点

## 交付

覆盖升级：`bash upgrade-existing.sh /opt/ISM/ism-release-oceanbase-20260817-0001-c851`，升完 Ctrl+F5。
