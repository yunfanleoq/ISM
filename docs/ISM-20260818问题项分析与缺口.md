# ISM 20260818 问题项：全量修复方案

> 需求来源：`20260818问题项(1).doc`  
> 对照：`docs/ISM-20260817问题项分析与修复方案.md`  
> 交付补丁：`releases/ism-patch-20260818-all-20260818-1855-2522`（含麒麟静态 `ism_server` + `web/dist`）

## 总表

| # | 现场问题 | 根因 | 修复状态 |
|---|---|---|---|
| 1 | 应用新增无法保存 / 导航不可用 | 现场旧 FE；预览态拦截静默；空 path | **已修**：最小 FE 加固 + 新 dist |
| 2a | 历史库备份失败 | `docker exec taosdump` 前未 `mkdir -p` | **已修**：`hisDbOpt.go` |
| 2b | 2208 点 / 600s 丢档 | 旧逻辑绑采集 | **已合包**：定时实时库快照 |
| 3 | 按位脚本无报警 | SettleAll 同值早退且启动窗无补推 | **已修**：SettleAll 跳过告警 + SyncAlarms |
| 4 | 虚拟设备导出无「报警触发值」列 | 源码已有，现场 dist/缓存旧 | **已重打** web/dist |
| 5 | 锁屏「服务不可用」卡死 | `/UserUnlock` 未注册 → 404 | **已修**：路由 + FE 逃生 |

## 代码改动摘要

### 后端

- `controllers/hisDbOpt.go`：容器内 `mkdir -p`，`docker exec -i`
- `routers/router.go`：注册 `/UserUnlock`
- `controllers/userCtl.go` / `models/user.go`：Password 安全断言；项目用户校验对齐登录
- `task/ISMScript/bitunpack/runtime.go`：SettleAll 用 SkipAlarm；`SyncAlarms`
- `task/ISMScript/func/deviceData.go`：`SetDeviceDataSkipAlarm`、队列丢包节流 Error
- `protocol/common/startupAlarmGuard.go` + `task/alarm/dealWithAlarm.go`：窗过期触发 SyncAlarms
- 定时快照：`timedRealtimeHistorySnapshot.go`（既有，编入合包二进制）

### 前端

- `LockScreen.vue`：动态错误文案 +「清除锁屏并重新登录」
- `ISMResources.vue` / `ISMHeader.vue`：新建页强制 `selectPageUuid`；预览保存提示
- `ISMRender.vue` / `ViewMenu.vue`：空 PageID / 未绑定 path 明确提示
- `VirtualDeviceModel.vue`：列「报警触发值(0,1)」随新 dist 发布

## 验收

见补丁包 `README-补丁说明.md`。

## 明确不做

- 不改密码 MD5→bcrypt 链路
- 不大改 ISMBase / 导航事件总线
- 大屏 KPI/用户状态以组态为主
- 不为告警回退 Anko 全量轮询
